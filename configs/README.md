# Configuration

Runtime configuration is validated environment-based Settings in app/config.py; see .env.example and compose.local.yaml. The hardware YAML is a descriptive evaluation profile, not an executable driver installation or a claim of device support. It maps to the System hardware status and must remain pending until device evidence exists. No ignored YAML values are presented as active runtime settings.
